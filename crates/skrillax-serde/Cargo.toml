[package]
name = "skrillax-serde"
version = "0.3.0"
authors = ["kumpelblase2"]
description = "A serialization/deserialization library for Silkroad Online packets."
license = "MIT"
repository = "https://git.eternalwings.de/tim/skrillax-network"
categories = ["encoding", "parser-implementations"]
keywords = ["silkroad", "serde"]
edition.workspace = true

[package.metadata.workspaces]
independent = true

[features]
default = ["chrono"]
derive = ["dep:skrillax-serde-derive"]
chrono = ["dep:chrono"]
_gen = ["dep:rand"]

[dependencies]
bytes.workspace = true
byteorder.workspace = true
skrillax-serde-derive = { path = "../skrillax-serde-derive", version = "0.3.0", optional = true }
chrono = { version = "^0.4.38", optional = true }
thiserror.workspace = true
rand = { workspace = true, optional = true }

[dev-dependencies]
bytes = "1.10"
criterion = "0.5"
rand.workspace = true

[[bench]]
name = "serde_bench"
harness = false
required-features = ["derive"]

[[bin]]
name = "generate_bench_data"
path = "dev/generate_data.rs"
required-features = ["derive", "_gen"]
