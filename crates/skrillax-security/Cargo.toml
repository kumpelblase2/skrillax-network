[package]
name = "skrillax-security"
version = "0.1.0"
authors = ["kumpelblase2"]
description = "Security implementation for Silkroad Online packet streams."
license = "MIT"
repository = "https://git.eternalwings.de/tim/skrillax-network"
categories = ["network-programming", "cryptography"]
keywords = ["silkroad", "security", "handshake"]
edition.workspace = true

[package.metadata.workspaces]
independent = true

[dependencies]
blowfish = { version = "0.9" }
bytes.workspace = true
byteorder.workspace = true
rand.workspace = true
thiserror.workspace = true
bitflags = "2"
